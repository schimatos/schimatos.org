export default ({}) => {

    // text display
    // input
    // conversion
    // upload
    // verification

    // (add option to do these in addition to defaults?)

    // When making shacls give only allowed constraints for example password will be limited to minLength, maxLength pattern and a true/false and most other values inputs will have no
    // other constraints, then use the current make path form for any other unkwnowns

    


    return {

    }
}